# Anthropic API key for Claude (one of these is required)
ANTHROPIC_API_KEY=
# Or use a Claude Max/Pro OAuth token instead (see README for setup)
# CLAUDE_CODE_OAUTH_TOKEN=

# Authentication password (required in production)
# If unset, WebSocket connections will be rejected unless DISABLE_AUTH=true
AUTH_PASSWORD=

# Optional: Disable authentication entirely (not recommended for production)
# DISABLE_AUTH=true

# Optional: Comma-separated list of allowed origins for CORS
# If not set, all origins are allowed
# ALLOWED_ORIGINS=https://example.com,https://app.example.com

# Server port (default: 3001)
# PORT=3001

# Optional: Data directory for trips/itineraries (default: ~/.travelagent)
# TRAVEL_AGENT_HOME=/absolute/path/to/data

# Optional: Nano Banana Pro image API integration (for generated trip map image)
# If unset, the server will generate a simple SVG placeholder.
# NANO_BANANA_PRO_API_URL=
# NANO_BANANA_PRO_API_KEY=

# Optional: Email notifications for scheduled reminders (requires Resend account)
# If unset, email reminders will be skipped (logged but not sent)
# RESEND_API_KEY=
# NOTIFICATION_EMAIL=your@email.com
# NOTIFICATION_FROM=noreply@yourdomain.com
